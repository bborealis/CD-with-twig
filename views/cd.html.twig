<html>
<head>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css'>
    <title>Welcome Page</title>
</head>
<body>
    <div class='container'>
        <h1>Welcome to your CD catalog!</h1>
        {% if cds is not empty %}
            <p> Here is a list of your CDs:</p>
            <ul>
                {% for cd in cds %}
                    <li>{{ cd.getArtist }} : {{ cd.getAlbum }}</li>
                {% endfor %}
            </ul>
        {% endif %}
        <!--took out the method='post', because this form is "getting", rather than "posting"-->
    <form action='/cds'>
        <button type='submit' class='btn-success'>Add a new CD</button>
    </form>

    {% if cds is not empty %}
    <form action='/searchbyartist'>
        <button type='submit' class='btn-success'>Search by artist</button>
    </form>
    <form action = 'delete_cd' method='post'>
        <button type='submit' class="btn-danger">Clear List</button>
    </form>
    {% endif %}
    </div>
</body>
</html>
